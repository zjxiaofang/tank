<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="pangu.mapper.UserDao">
    <select id="insertOne">
        select * from LoginUser where login_name=? and password = ?
    </select>
    //保存
    <insert id="saveUser" parameterType="pangu.domain.BasicInformation">
        insert into basic_information values(#{id},#{gmtCreate},#{gmtModified},#{username},#{birthday},#{sex},#{address},#{phoneNumber},#{mail},#{identity},#{workTime},#{introduce},#{userID});
    </insert>
</mapper>